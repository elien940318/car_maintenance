{
  "name": "carmaint",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm --parallel -r build",
    "test": "pnpm --parallel -r test"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=11"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
